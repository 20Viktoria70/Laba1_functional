# Завдання 4. Рекурсивна довжина списку
# Реалізувати функцію myLength, яка рахує довжину списку без використання вбудованої функції length. Приклад: myLength [1,2,3,4] == 4

myLength :: [a] -> Int
myLength [] = 0
myLength (_:xs) = 1 + myLength xs

main :: IO ()
main = do
    putStrLn "Введіть числа через пробіл:"
    input <- getLine
    let nums = map read (words input) :: [Int]
    let len = myLength nums
    putStrLn ("Довжина списку: " ++ show len)
