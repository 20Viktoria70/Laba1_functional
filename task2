# Завдання 2. Сума непарних чисел
# Написати функцію sumOdd, яка обчислює суму всіх непарних чисел у списку. Приклад: sumOdd [1,2,3,4,5] == 9


# функція, яка приймає список чисел
def sum_odd(lst):
    
    #sum_odd рахує суму
    #перебираэмо кожне число x у списку lst
     # умова (x % 2 != 0) залишає тільки непарні числа
    # у підсумку в суму потраплять лише непарні
    
    return sum(x for x in lst if x % 2 != 0)

# приклад
print(sum_odd([1, 2, 3, 4, 5])) 
