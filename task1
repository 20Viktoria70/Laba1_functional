# Написати функцію doubleEven, яка для списку цілих чисел подвоює тільки парні числа, залишаючи непарні без змін. Приклад:doubleEven [1,2,3,4] == [1,4,3,8]


# функція, яка приймає список чисел
def double_even(lst):
    #створюю новий списо за допомогою list comprehension
    #перебираємо кожен елемент x зі списку lst
    # якщо x парне (x % 2 == 0), беремо x*2
    # інакше (x непарне) залишаємо x без змін
    return [x * 2 if x % 2 == 0 else x for x in lst]

# приклад роботи програми
print(double_even([1, 2, 3, 4]))
